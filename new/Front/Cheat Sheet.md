# CSS父子元素关系速查表

## 🎯 一、5秒快速判断法
> **问自己：这属性跟文字样式有关吗？**
> - **是** → 通常**会继承**（如颜色、字体）
> - **否** → 通常**不继承**（如尺寸、边距）

---

## 📋 二、属性继承速查表

### ✅ **【会继承】文字样式类**
```css
/* 这些子元素自动获得父元素的值 */
color                文字颜色
font-family          字体
font-size            字体大小
font-weight          粗细 (bold/normal)
font-style           样式 (italic/normal)
line-height          行高
text-align           对齐 (left/center/right)
text-indent          首行缩进
text-transform       大小写转换
letter-spacing       字间距
word-spacing         词间距
visibility           可见性
cursor               鼠标指针
list-style           列表样式
quotes               引号样式
```

### ❌ **【不继承】布局盒子类**
```css
/* 这些必须给子元素单独设置 */
width/height         宽高
margin               外边距
padding              内边距
border               边框
background           背景
display              显示方式
position             定位
float/clear          浮动
overflow             溢出处理
vertical-align       垂直对齐
z-index              层叠顺序
opacity              透明度
box-sizing           盒模型
```

---

## 📊 三、百分比计算基准表

### **基准 = 父元素宽度**
```css
margin-top: 10%        → 父宽度 × 10%
margin-bottom: 10%     → 父宽度 × 10%
padding-top: 10%       → 父宽度 × 10%
padding-bottom: 10%    → 父宽度 × 10%
left: 20%              → 父宽度 × 20%
right: 20%             → 父宽度 × 20%
```

### **基准 = 父元素对应尺寸**
```css
width: 50%             → 父内容宽度 × 50%
height: 50%            → 父内容高度 × 50%
top: 20%               → 父高度 × 20%
bottom: 20%            → 父高度 × 20%
font-size: 150%        → 父字体大小 × 150%
```

### **基准 = 自身宽度**
```css
padding: 5%            → 自身宽度 × 5%  (四个方向都是!)
border-radius: 50%     → 自身尺寸 × 50%
```

---

## 🔧 四、常见问题急救表

| 问题现象 | 原因 | 解决方法 |
|---------|------|---------|
| `height: 100%` 无效 | 父元素没高度 | 1. 给父元素设固定高度<br>2. 用 `height: 100vh`<br>3. 改用 `flex` 布局 |
| 子元素超出父容器 | 子元素太宽或有padding | 1. 父元素加 `overflow: auto`<br>2. 子元素加 `box-sizing: border-box`<br>3. 子元素设 `max-width: 100%` |
| 垂直margin合并 | 上下相邻元素margin接触 | 1. 父元素加 `overflow: auto`<br>2. 用padding代替margin<br>3. 加 `border: 1px solid transparent` |
| 绝对定位错位 | 没找到定位参照 | 1. 父元素加 `position: relative`<br>2. 检查祖先元素定位 |

---

## 🚀 五、布局关系速查

### **Flex布局中**
```css
.parent {
  display: flex;          /* 子元素自动变成flex项 */
}
.child {
  flex: 1;                /* 子项特有属性，父元素没有 */
  align-self: center;     /* 单个子项对齐方式 */
}
```

### **Grid布局中**
```css
.parent {
  display: grid;          /* 子元素自动变成grid项 */
  grid-template-columns: 1fr 1fr; /* 父定义网格 */
}
.child {
  grid-column: 1 / 3;     /* 子项可跨越网格 */
}
```

### **定位关系中**
```css
.parent {
  position: relative;     /* 成为子元素的定位参照 */
}
.child {
  position: absolute;     /* 相对于最近的非static祖先 */
  top: 0;                 /* 距离参照元素顶部0px */
  left: 0;                /* 距离参照元素左侧0px */
}
```

---

## 💡 六、魔法关键字表

| 关键字 | 作用 | 示例 |
|--------|------|------|
| `inherit` | **强制继承**父元素值 | `border: inherit;` |
| `initial` | 重置为**浏览器默认** | `display: initial;` |
| `unset` | 如果是继承属性则继承，否则重置 | `color: unset;` |
| `revert` | 重置为**用户代理样式** | 很少用 |

---

## 🎪 七、特殊场景速记

### **场景1：想让子元素和父元素一样宽**
```css
/* ✅ 正确做法 */
.child {
  width: 100%;           /* 基于父元素内容宽度 */
  box-sizing: border-box; /* padding/border包含在内 */
}

/* ❌ 常见错误 */
.child {
  width: inherit;        /* 有时有效，但父元素必须有明确宽度 */
}
```

### **场景2：垂直居中**
```css
/* 方法1：Flex方案（推荐） */
.parent {
  display: flex;
  align-items: center;    /* 垂直居中 */
  justify-content: center; /* 水平居中 */
}

/* 方法2：Grid方案 */
.parent {
  display: grid;
  place-items: center;    /* 水平和垂直都居中 */
}

/* 方法3：传统方案 */
.parent {
  position: relative;
}
.child {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
```

### **场景3：子元素间距**
```css
/* ✅ 现代方案 */
.parent {
  display: flex;
  gap: 20px;              /* 只作用于子元素之间 */
}

/* ❌ 传统方案 */
.parent .child {
  margin-right: 20px;     /* 最后一个元素会有多余边距 */
}
.parent .child:last-child {
  margin-right: 0;        /* 需要额外清除 */
}
```

---

## 📝 八、必备代码片段

### **放在所有CSS开头**
```css
/* 避免盒模型计算混乱 */
*,
*::before,
*::after {
  box-sizing: border-box;
}

/* 基础样式让子元素继承 */
body {
  font-family: system-ui, -apple-system, sans-serif;
  line-height: 1.6;
  color: #333;
}
```

### **快速调试边框**
```css
/* 临时查看元素边界 */
.debug * { outline: 1px solid red; }
.debug .parent { background: rgba(255,0,0,0.1); }
.debug .child { background: rgba(0,255,0,0.1); }
```

---

## 🧠 九、终极记忆口诀

### **【三句话记住核心】**
1. **字传盒不传** - 文字样式传，盒子样式不传
2. **百分看基准** - 百分比都找"爹"（父元素）算
3. **高度要明确** - 父无明确高，子百分比高无效

### **【快速检查清单】**
遇到布局问题时，按顺序检查：
1. ✅ 父元素有宽度/高度吗？
2. ✅ 用了正确的盒模型吗？(`box-sizing: border-box`)
3. ✅ 百分比基准对吗？
4. ✅ 需要特殊定位参照吗？(`position: relative`)

---

**保存这张表，遇到问题时快速查阅！** 大多数CSS布局问题都能在表中找到答案。